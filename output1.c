#include <stdio.h>
    #include<math.h>
int main ()
    {
    int num;

    printf("\nEnter 5 digit number");
        scanf ("%d", &num);

        if (num%10)
        {
            if (num=num%100000)
            printf("the reverse number is:%d", num);
            
        }
        
        else if (num==num)
        {
            printf("\n Number =%d ",num);
        }
        
    }
